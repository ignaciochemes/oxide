# Artefactos de compilación de Rust
/target

# Node / Next.js (dashboard)
node_modules/
web/.next/
web/out/
web/next-env.d.ts
*.tsbuildinfo

# Lockfile de Rust: para binarios conviene comitearlo, lo dejamos versionado.
# (descomentá la línea de abajo si querés ignorarlo)
# Cargo.lock
